= Changelog
:toc: left
:toc-title: Versions
:toclevels: 2

== v3.2.1.5 — 2026-07-29

* Rename the internal source dir `src/asciidoctor_diagram/` → `src/asciicrystal_diagram/` (internal path; no API change). Ruby-gem provenance preserved.

== v3.2.1.4 — 2026-07-29

* Remove the non-functional CLI target (`main` pointed at an uncommitted build artifact); this shard is a library extension, like `asciicrystal-mathematical`.

== v3.2.1.3 — 2026-07-28

* Rename the shard `crystal-asciidoctor-diagram` -> `asciicrystal-diagram` and the module `AsciidoctorDiagram` -> `AsciicrystalDiagram`, aligned with the core shard renamed to `asciicrystal`.

== v3.2.1.1 — 2026-04-22

* Add the *ameba* linter to the CI pipeline with a shared `.ameba.yml` config aligned with the Crystal ecosystem.
* Fix two `Lint/RedundantStringCoercion` violations: drop explicit `.to_s` calls in string interpolations in `generator.cr`.
